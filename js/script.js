//AP'S STATE (vaiables that change)
let board;
let playerTurn;
let Winner;

// CASHED ELEMENT REFERENCES
let gameBoard = document.getElementById('mancala-board')
let reStartButton = document.getElementById('restart-button')

//EVENT LISTENERS


// FUNCTIONS
function init(){
    playerTurn = 1
    board = [4, 4, 4, 4, 4, 4, 0, 4, 4, 4, 4, 4, 4, 0]
}

if(board[index]!==6 && board[index] !==13){
    board.forEach(function(index){
        addEventListener
    })

    
}
